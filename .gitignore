__pycache__/
*.py[cod]

# oh-my-claudecode session state (session ids, local transcript paths)
.omc/

# written by `claude-pet update` on installs that have no git history
.claude-pet-version

# `preview` / `snapshot` output
*-preview.png
*.snapshot.png

.venv/
